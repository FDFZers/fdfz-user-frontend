# ffwiki server

Go backend for ffwiki.

## Directory structure

- `cmd/server` — application entry point (main package)
- `internal` — private application code (handlers, services, store, etc.)
- `pkg` — shared/libraries that can be used by external projects
